# ============================================
# LongPort OpenAPI 配置
# ============================================
# 从 LongPort 开放平台获取：https://open.longbridge.com
# 文档：https://open.longbridge.com/zh-CN/docs/getting-started
LONGPORT_APP_KEY=your_app_key_here
LONGPORT_APP_SECRET=your_app_secret_here
LONGPORT_ACCESS_TOKEN=your_access_token_here

# ============================================
# 交易标的配置
# ============================================
# 监控标的（用于计算指标和生成交易信号）
# 示例：HSI.HK（恒生指数）
MONITOR_SYMBOL=HSI.HK

# 做多标的（不带 .HK 后缀，内部会自动规范为港股）
# 当监控标的产生 BUY 信号时，买入此标的（做多操作）
# 示例：68547（恒指牛证）
LONG_SYMBOL=68547

# 做空标的（不带 .HK 后缀，内部会自动规范为港股）
# 当监控标的产生 SELL 信号时，买入此标的（做空操作）
# 示例：63372（恒指熊证）
SHORT_SYMBOL=63372

# ============================================
# 交易金额和仓位配置
# ============================================
# 目标买入金额（HKD），会按 <= 此金额且尽量接近的方式计算股数
TARGET_NOTIONAL=5000

# 做多标的的最小买卖单位（每手股数，作为后备值，优先使用从API获取的值）
LONG_LOT_SIZE=100

# 做空标的的最小买卖单位（每手股数，作为后备值，优先使用从API获取的值）
SHORT_LOT_SIZE=100

# ============================================
# 风险管理配置
# ============================================
# 单标的最大持仓市值（HKD），不允许超过此金额
MAX_POSITION_NOTIONAL=100000

# 单日最大亏损（HKD），超过后禁止继续开新仓
MAX_DAILY_LOSS=30000

# 是否在收盘前15分钟清空所有持仓
# 港股当日收盘时间：下午 16:00
# 收盘前15分钟：15:45-16:00（仅判断当日收盘，不包括上午收盘）
# 设置为 "true" 启用，其他值或不设置则禁用
CLEAR_POSITIONS_BEFORE_CLOSE=true

# ============================================
# 调试配置
# ============================================
# 是否启用 DEBUG 日志（设置为 "true" 启用，其他值或不设置则禁用）
DEBUG=false
